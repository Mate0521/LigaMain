

# Desactivar listado de directorios
Options -Indexes

# Página por defecto
DirectoryIndex index.php

# Bloquear acceso a archivos ocultos (.git, .env, etc.)
<FilesMatch "^\.">
  Require all denied
</FilesMatch>

# Bloquear acceso directo a archivos sensibles específicos
<FilesMatch "(config\.php|dao\.php|modelo\.php|component\.php)$">
  Require all denied
</FilesMatch>

# Activar el motor de reescritura
RewriteEngine On

# Ignorar archivos o carpetas que existan físicamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirigir todo a index.php
RewriteRule ^([^/]+)/?$ index.php?page=$1 [L,QSA]


